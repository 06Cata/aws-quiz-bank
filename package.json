{
  "name": "aws-quiz-bank",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev:web": "npm --workspace apps/web run dev",
    "build:web": "npm --workspace apps/web run build",
    "lint:web": "npm --workspace apps/web run lint",
    "dev:api": "cd apps/api && uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "sync:questions": "cd apps/api && QUIZ_EXAM=saa python3 -m app.jobs.sync_local_questions",
    "sync:questions:saa": "cd apps/api && QUIZ_EXAM=saa python3 -m app.jobs.sync_local_questions",
    "validate:questions": "cd apps/api && python3 -m app.services.local_questions ../../questions --exam saa"
  }
}
