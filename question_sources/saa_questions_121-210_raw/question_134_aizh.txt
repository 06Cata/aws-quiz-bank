A company wants to move its application to a serverless solution. The serverless solution needs to analyze existing and new data
by using SL. The company stores the data in an Amazon S3 bucket. The data requires encryption and must be replicated to a
different AWS Region.
Which solution will meet these requirements with the LEAST operational overhead?
一家公司希望将其应用程序迁移到无服务器解决方案。该无服务器解决方案需要使用 SL 分析现有和新数据。公司将数据存储在
Amazon S3 bucket 中。数据需要加密，并且必须复制到不同的 AWS 区域。
哪种解决方案能够以最少的运营开销满足这些需求？
A. Create a new S3 bucket. Load the data into the new S3 bucket. Use S3 Cross-Region Replication (CRR) to replicate encrypted
objects to an S3 bucket in another Region. Use server-side encryption with AWS KMS multi-Region kays (SSE-KMS). Use
Amazon Athena to query the data.
创建一个新的 S3 bucket。将数据加载到新的 S3 bucket 中。使用 S3 Cross-Region Replication (CRR) 将加密对象复制到另一个
区域的 S3 bucket 中。使用 AWS KMS 多区域密钥的服务器端加密 (SSE-KMS)。使用 Amazon Athena 查询数据。
B. Create a new S3 bucket. Load the data into the new S3 bucket. Use S3 Cross-Region Replication (CRR) to replicate encrypted
objects to an S3 bucket in another Region. Use server-side encryption with AWS KMS multi-Region keys (SSE-KMS). Use
Amazon RDS to query the data.
创建一个新的 S3 bucket。将数据加载到新的 S3 bucket 中。使用 S3 Cross-Region Replication (CRR) 将加密对象复制到另一个
区域的 S3 bucket 中。使用 AWS KMS 多区域密钥的服务器端加密 (SSE-KMS)。使用 Amazon RDS 查询数据。
C. Load the data into the existing S3 bucket. Use S3 Cross-Region Replication (CRR) to replicate encrypted objects to an S3
bucket in another Region. Use server-side encryption with Amazon S3 managed encryption keys (SSE-S3). Use Amazon Athena
to query the data. Most Voted
将数据加载到现有的 S3 bucket 中。使用 S3 Cross-Region Replication (CRR) 将加密对象复制到另一个区域的 S3 bucket 中。使
用 Amazon S3 托管加密密钥的服务器端加密 (SSE-S3)。使用 Amazon Athena 查询数据。
D. Load the data into the existing S3 bucket. Use S3 Cross-Region Replication (CRR) to replicate encrypted objects to an S3
bucket in another Region. Use server-side encryption with Amazon S3 managed encryption keys (SSE-S3). Use Amazon RDS to
query the data.
将数据加载到现有的 S3 bucket 中。使用 S3 Cross-Region Replication (CRR) 将加密对象复制到另一个区域的 S3 bucket 中。使
用 Amazon S3 托管加密密钥的服务器端加密 (SSE-S3)。使用 Amazon RDS 查询数据。
Correct Answer: C
正确答案：C
题目分析与解答
1. 考察的知识点
Amazon S3的加密选项（SSE-KMS vs SSE-S3）。
S3 Cross-Region Replication (CRR)的功能和配置。
使用Amazon Athena进行数据查询的能力。
选择最少的操作开销（serverless架构）。
2. 选项分析
A:
正确点：使用S3 Cross-Region Replication (CRR)来实现跨区域复制，符合要求。
正确点：使用AWS KMS多区域密钥（SSE-KMS）进行加密，符合加密要求。
正确点：使用Amazon Athena查询数据，符合serverless架构要求。
错误点：创建新的S3 bucket增加了额外的操作开销，不符合“最少操作开销”的要求。
B:
正确点：使用S3 Cross-Region Replication (CRR)来实现跨区域复制，符合要求。
正确点：使用AWS KMS多区域密钥（SSE-KMS）进行加密，符合加密要求。
错误点：使用Amazon RDS查询数据不符合serverless架构要求，因为RDS需要管理实例，增加了操作开销。
错误点：创建新的S3 bucket增加了额外的操作开销，不符合“最少操作开销”的要求。
C:
正确点：使用现有的S3 bucket减少了操作开销，符合“最少操作开销”的要求。
正确点：使用S3 Cross-Region Replication (CRR)来实现跨区域复制，符合要求。
正确点：使用Amazon S3托管加密密钥（SSE-S3）进行加密，符合加密要求。
正确点：使用Amazon Athena查询数据，符合serverless架构要求。
D:
正确点：使用现有的S3 bucket减少了操作开销，符合“最少操作开销”的要求。
正确点：使用S3 Cross-Region Replication (CRR)来实现跨区域复制，符合要求。
正确点：使用Amazon S3托管加密密钥（SSE-S3）进行加密，符合加密要求。
错误点：使用Amazon RDS查询数据不符合serverless架构要求，因为RDS需要管理实例，增加了操作开销。
3. 我选的答案
C
4. 官方答案
C
5. 与官方答案比较
我的答案与官方答案一致，无需重新思考。
Community vote distribution
社区投票分布
C (54%)
A (46%)
棃孨: " IT认证轻松过 " https://shop542998714.m.taobao.com
嬄信: " Examtopics "
