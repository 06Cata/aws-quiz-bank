An application allows users at a company's headquarters to access product data. The product data is stored in an Amazon RDS
MySQL DB instance. The operations team has isolated an application performance slowdown and wants to separate read traffic
from write traffic. A solutions architect needs to optimize the application's performance quickly.
What should the solutions architect recommend?
一个应用程序允许公司总部的用户访问产品数据。产品数据存储在一个 Amazon RDS MySQL DB 实例中。运营团队已经隔离了应用
程序性能变慢的问题，并希望将读流量与写流量分离。解决方案架构师需要快速优化应用程序的性能。
解决方案架构师应该推荐什么？
A. Change the existing database to a Multi-AZ deployment. Serve the read requests from the primary Availability Zone.
将现有数据库更改为 Multi-AZ 部署。从主可用区处理读请求。
B. Change the existing database to a Multi-AZ deployment. Serve the read requests from the secondary Availability Zone.
将现有数据库更改为 Multi-AZ 部署。从次可用区处理读请求。
C. Create read replicas for the database. Configure the read replicas with half of the compute and storage resources as the
source database.
为数据库创建只读副本。将只读副本配置为源数据库一半的计算和存储资源。
D. Create read replicas for the database. Configure the read replicas with the same compute and storage resources as the
source database. Most Voted
为数据库创建只读副本。将只读副本配置为与源数据库相同的计算和存储资源。
题目分析与解答
1. 考察的知识点
Amazon RDS 的读写分离功能。
Correct Answer: D
正确答案: D
Community vote distribution
社区投票分布
D (99%)
1
使用 Read Replica 优化数据库性能。
理解 Multi-AZ 部署的用途和限制。
2. 选项分析
A. Change the existing database to a Multi-AZ deployment. Serve the read requests from the primary Availability Zone.
错误：Multi-AZ 部署的主要目的是提供高可用性和灾难恢复，而不是优化性能。主可用区仍然处理所有读写请求，无法实现读写分
离。
B. Change the existing database to a Multi-AZ deployment. Serve the read requests from the secondary Availability Zone.
错误：Multi-AZ 的备用实例仅用于故障转移，不能直接处理读请求。此选项不符合读写分离的需求。
C. Create read replicas for the database. Configure the read replicas with half of the compute and storage resources as the
source database.
错误：虽然创建 Read Replica 是正确的方向，但配置副本的资源为源数据库的一半可能会导致性能瓶颈，尤其是在高负载场景下。
D. Create read replicas for the database. Configure the read replicas with the same compute and storage resources as the
source database.
正确：创建 Read Replica 可以实现读写分离，优化性能。配置副本与源数据库相同的资源可以确保副本能够处理高负载的读请求。
3. 我的选择
D. Create read replicas for the database. Configure the read replicas with the same compute and storage resources as the source
database.
4. 官方答案
D. Create read replicas for the database. Configure the read replicas with the same compute and storage resources as the source
database.
5. 与官方答案的比较
我的答案与官方答案一致，没有差异。
棃孨: " IT认证轻松过 " https://shop542998714.m.taobao.com
嬄信: " Examtopics "
