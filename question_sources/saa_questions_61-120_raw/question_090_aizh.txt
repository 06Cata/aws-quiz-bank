A company is using a SQL database to store movie data that is publicly accessible. The database runs on an Amazon RDS Single-
AZ DB instance. A script runs queries at random intervals each day to record the number of new movies that have been added to
the database. The script must report a final total during business hours.
The company's development team notices that the database performance is inadequate for development tasks when the script is
running. A solutions architect must recommend a solution to resolve this issue.
Which solution will meet this requirement with the LEAST operational overhead?
一家公司正在使用一个 SQL 数据库来存储公开访问的电影数据。该数据库运行在一个 Amazon RDS 单可用区 DB 实例上。一个脚本
每天会在随机时间间隔运行查询，以记录添加到数据库中的新电影数量。该脚本必须在工作时间内报告最终总数。
该公司的开发团队注意到，当脚本运行时，数据库性能不足以支持开发任务。一位解决方案架构师必须推荐一个解决方案来解决此
问题。
哪个解决方案可以以最少的运营开销满足此需求？
A. Modify the DB instance to be a Multi-AZ deployment.
将 DB 实例修改为多可用区部署。
B. Create a read replica of the database. Configure the script to query only the read replica. Most Voted
创建数据库的只读副本。配置脚本仅查询只读副本。
C. Instruct the development team to manually export the entries in the database at the end of each day.
指示开发团队在每天结束时手动导出数据库中的条目。
D. Use Amazon ElastiCache to cache the common queries that the script runs against the database.
使用 Amazon ElastiCache 缓存脚本针对数据库运行的常见查询。
题目分析与解答
1. 考察的知识点
Correct Answer: B
正确答案：B
Community vote distribution
社区投票分布
B (95%)
3%
Amazon RDS的功能，包括单可用区和多可用区部署。
Amazon RDS的读副本（Read Replica）功能及其用途。
Amazon ElastiCache的使用场景。
优化数据库性能的解决方案。
2. 选项分析
A. Modify the DB instance to be a Multi-AZ deployment.
多可用区部署主要用于提高数据库的可用性和容错能力，而不是直接提升性能。
虽然可以提供冗余，但不会解决脚本运行时对性能的影响。
此选项错误。
B. Create a read replica of the database. Configure the script to query only the read replica.
读副本用于分担主数据库的读取负载，适合解决性能问题。
脚本可以查询读副本，从而减少对主数据库的压力。
此选项正确。
C. Instruct the development team to manually export the entries in the database at the end of each day.
手动导出数据增加了操作复杂性，且不符合“最少操作开销”的要求。
此选项错误。
D. Use Amazon ElastiCache to cache the common queries that the script runs against the database.
ElastiCache适合缓存频繁查询的数据，但题目中脚本运行的查询是随机的，无法有效利用缓存。
此选项错误。
3. 我的答案
B. Create a read replica of the database. Configure the script to query only the read replica.
4. 官方答案
B. Create a read replica of the database. Configure the script to query only the read replica.
5. 与官方答案比较
我的答案与官方答案一致，无需重新思考。
棃孨: " IT认证轻松辟 " https://shop542998714.m.taobao.com
咸轼: " IT认证轻松辟 " https://www.goofish.com/personal?userId=2645260430
嬄信: " Examtopics "
