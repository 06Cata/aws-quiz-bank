A company is developing a two-tier web application on AWS. The company's developers have deployed the application on an
Amazon EC2 instance that connects directly to a backend Amazon RDS database. The company must not hardcode database
credentials in the application. The company must also implement a solution to automatically rotate the database credentials on a
regular basis.
Which solution will meet these requirements with the LEAST operational overhead?
一家公司正在 AWS 上开发一个两层的 Web 应用程序。公司的开发人员已经将应用程序部署在一个直接连接到后端 Amazon RDS 数
据库的 Amazon EC2 实例上。公司不得在应用程序中硬编码数据库凭证。公司还必须实施一个解决方案，以定期自动轮换数据库凭
证。
哪种解决方案能够以最少的操作开销满足这些要求？
A. Store the database credentials in the instance metadata. Use Amazon EventBridge (Amazon CloudWatch Events) rules to run
a scheduled AWS Lambda function that updates the RDS credentials and instance metadata at the same time.
将数据库凭证存储在实例元数据中。使用 Amazon EventBridge (Amazon CloudWatch Events) 规则运行一个计划的 AWS
Lambda 函数，同时更新 RDS 凭证和实例元数据。
B. Store the database credentials in a configuration file in an encrypted Amazon S3 bucket. Use Amazon EventBridge (Amazon
CloudWatch Events) rules to run a scheduled AWS Lambda function that updates the RDS credentials and the credentials in the
configuration file at the same time. Use S3 Versioning to ensure the ability to fall back to previous values.
将数据库凭证存储在加密的 Amazon S3 存储桶中的配置文件中。使用 Amazon EventBridge (Amazon CloudWatch Events) 规则
运行一个计划的 AWS Lambda 函数，同时更新 RDS 凭证和配置文件中的凭证。使用 S3 Versioning 确保能够回滚到之前的值。
C. Store the database credentials as a secret in AWS Secrets Manager. Turn on automatic rotation for the secret. Attach the
required permission to the EC2 role to grant access to the secret. Most Voted
将数据库凭证作为一个 secret 存储在 AWS Secrets Manager 中。开启 secret 的自动轮换功能。将所需权限附加到 EC2 的角色，
以授予访问该 secret 的权限。
D. Store the database credentials as encrypted parameters in AWS Systems Manager Parameter Store. Turn on automatic
rotation for the encrypted parameters. Attach the required permission to the EC2 role to grant access to the encrypted
parameters.
将数据库凭证作为加密参数存储在 AWS Systems Manager Parameter Store 中。开启加密参数的自动轮换功能。将所需权限附
加到 EC2 的角色，以授予访问这些加密参数的权限。
Correct Answer: C
正确答案：C
题目分析与解答
1. 考察的知识点
如何安全地存储和管理数据库凭证。
如何实现自动轮换数据库凭证以减少运维工作量。
使用 AWS 服务（如 Secrets Manager 和 Systems Manager）来管理敏感信息。
2. 选项分析
A. 将数据库凭证存储在实例元数据中，并使用 EventBridge 和 Lambda 更新：
错误：实例元数据不适合存储敏感信息，因为它的安全性较低，且无法直接支持自动轮换功能。
错误：需要手动编写 Lambda 函数和 EventBridge 规则，增加了运维复杂性。
B. 将数据库凭证存储在加密的 S3 配置文件中，并使用 EventBridge 和 Lambda 更新：
错误：虽然 S3 可以加密存储，但它不是专门设计用于管理敏感信息的服务。
错误：需要手动编写 Lambda 函数和 EventBridge 规则，增加了运维复杂性。
错误：S3 Versioning虽然提供了回滚功能，但无法直接支持自动轮换。
C. 将数据库凭证存储在 AWS Secrets Manager 中，并开启自动轮换功能：
正确：Secrets Manager 专门设计用于存储和管理敏感信息，并支持自动轮换功能。
正确：通过为 EC2 实例附加适当的 IAM 权限，可以安全地访问存储的凭证。
正确：此解决方案具有最低的运维开销，因为自动轮换功能由 AWS 完全管理。
D. 将数据库凭证存储在 AWS Systems Manager Parameter Store 中，并开启自动轮换功能：
错误：虽然 Parameter Store 可以存储加密参数，但它不支持自动轮换功能。
错误：需要手动实现轮换逻辑，增加了运维复杂性。
3. 我选的答案
C
4. 官方答案
C
5. 与官方答案比较
我的答案与官方答案一致。
原因：Secrets Manager 是专门设计用于存储敏感信息的服务，并且支持自动轮换功能，满足题目要求并具有最低的运维开销。
Community vote distribution
社区投票分布
C (100%)
棃宝: " IT认证轻松过 " https://shop542998714.m.taobao.com
