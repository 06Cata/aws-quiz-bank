Question #465
A company is developing an application to support customer demands. The company wants to deploy the application on multiple
Amazon EC2 Nitro-based instances within the same Availability Zone. The company also wants to give the application the ability
to write to multiple block storage volumes in multiple EC2 Nitro-based instances simultaneously to achieve higher application
availability.
Which solution will meet these requirements?
一家公司正在开发一个应用程序以支持客户需求。该公司希望将应用程序部署在同一可用区内的多个基于 Amazon EC2 Nitro 的实
例上。公司还希望赋予应用程序同时写入多个基于 EC2 Nitro 的实例中的多个块存储卷的能力，以实现更高的应用程序可用性。
哪种解决方案可以满足这些需求？
A. Use General Purpose SSD (gp3) EBS volumes with Amazon Elastic Block Store (Amazon EBS) Multi-Attach
使用 General Purpose SSD (gp3) EBS 卷与 Amazon Elastic Block Store (Amazon EBS) Multi-Attach
B. Use Throughput Optimized HDD (st1) EBS volumes with Amazon Elastic Block Store (Amazon EBS) Multi-Attach
使用 Throughput Optimized HDD (st1) EBS 卷与 Amazon Elastic Block Store (Amazon EBS) Multi-Attach
C. Use Provisioned IOPS SSD (io2) EBS volumes with Amazon Elastic Block Store (Amazon EBS) Multi-Attach Most Voted
使用 Provisioned IOPS SSD (io2) EBS 卷与 Amazon Elastic Block Store (Amazon EBS) Multi-Attach
D. Use General Purpose SSD (gp2) EBS volumes with Amazon Elastic Block Store (Amazon EBS) Multi-Attach
使用 General Purpose SSD (gp2) EBS 卷与 Amazon Elastic Block Store (Amazon EBS) Multi-Attach
题目分析与解答
1. 考察的知识点
Correct Answer: C
正确答案: C
Community vote distribution
社区投票分布
C (95%)
2%
Amazon EC2 Nitro-based instances的特性。
Amazon Elastic Block Store (Amazon EBS) Multi-Attach功能的使用。
不同类型的EBS卷（gp3、st1、io2、gp2）的性能和适用场景。
2. 选项分析
A. Use General Purpose SSD (gp3) EBS volumes with Amazon Elastic Block Store (Amazon EBS) Multi-Attach
错误：虽然gp3是通用型SSD卷，提供高性能和低成本，但它不支持EBS Multi-Attach功能。
B. Use Throughput Optimized HDD (st1) EBS volumes with Amazon Elastic Block Store (Amazon EBS) Multi-Attach
错误：st1是优化吞吐量的HDD卷，适用于大规模顺序读写工作负载，但它不支持EBS Multi-Attach功能。
C. Use Provisioned IOPS SSD (io2) EBS volumes with Amazon Elastic Block Store (Amazon EBS) Multi-Attach
正确：io2是高性能的SSD卷，支持EBS Multi-Attach功能，允许多个EC2实例同时挂载同一个卷，满足题目要求的高可用性和多实例
写入能力。
D. Use General Purpose SSD (gp2) EBS volumes with Amazon Elastic Block Store (Amazon EBS) Multi-Attach
错误：gp2是通用型SSD卷，但它不支持EBS Multi-Attach功能。
3. 我的答案
C. Use Provisioned IOPS SSD (io2) EBS volumes with Amazon Elastic Block Store (Amazon EBS) Multi-Attach
4. 官方答案
C. Use Provisioned IOPS SSD (io2) EBS volumes with Amazon Elastic Block Store (Amazon EBS) Multi-Attach
5. 与官方答案比较
我的答案与官方答案一致，没有差异。