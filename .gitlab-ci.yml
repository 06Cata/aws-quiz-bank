stages:
  - sync

sync_google_sheet:
  stage: sync
  image: python:3.12-slim
  rules:
    - if: '$CI_PIPELINE_SOURCE == "schedule"'
    - if: '$CI_PIPELINE_SOURCE == "web"'
  before_script:
    - cd apps/api
    - pip install -r requirements.txt
  script:
    - python -m app.jobs.sync_google_sheet
